<?xml version="1.0"?>
<doc>
    <assembly>
        <name>MAIMS.Data</name>
    </assembly>
    <members>
        <member name="T:MAIMS.Data.AssetCodeGeneration.SequentialAssetCodeGenerator">
            <summary>
            Default asset code generator. Scheme: DEPT_CODE-CATEGORY_CODE-SEQ (5 digits).
            Sequence is per (department, category) and derived from the highest existing
            sequence number for that pair. Production should replace this with a SQL
            sequence or a counter table locked under SERIALIZABLE for concurrency safety.
            </summary>
        </member>
        <member name="T:MAIMS.Data.Configurations.ModelBuilderExtensions">
            <summary>
            Centralised EF Core Fluent API configurations. Keeping them in one file
            per aggregate keeps the DbContext OnModelCreating short and reviewable.
            Conventions enforced here: BIGINT UNSIGNED PKs, utf8mb4, FK cascading rules,
            indexes on hot lookup paths.
            </summary>
        </member>
        <member name="M:MAIMS.Data.Configurations.ModelBuilderExtensions.ApplyAuditColumns``1(Microsoft.EntityFrameworkCore.Metadata.Builders.EntityTypeBuilder{``0})">
            <summary>
            Maps the common audit columns (created_at, updated_at, created_by, updated_by,
            is_deleted, deleted_at) for any entity implementing IAuditable + ISoftDeletable.
            </summary>
        </member>
        <member name="T:MAIMS.Data.Interceptors.AuditSaveChangesInterceptor">
             <summary>
             EF Core interceptor that automatically:
               1. Fills CreatedAt/CreatedBy/UpdatedAt/UpdatedBy on every auditable entity.
               2. Sets IsDeleted/DeletedAt on soft-delete (when state == Deleted).
               3. Writes an immutable AuditLog row for every CUD operation on tracked entities.
            
             Audit log entries are written in SavedChangesAsync (AFTER SaveChanges)
             so that auto-increment IDs are already populated by the database. The list
             of changed entries is captured in SavingChangesAsync (BEFORE SaveChanges)
             into a per-call list, then re-walked in SavedChangesAsync to read the
             now-populated IDs.
            
             This avoids the previous bug where IDs were read as 0 (or, after JSON
             serialization, appeared as negative numbers) because auto-increment IDs
             are only assigned by the DB at INSERT time.
             </summary>
        </member>
        <member name="M:MAIMS.Data.Interceptors.AuditSaveChangesInterceptor.ApplyAuditFieldsAndSnapshot(Microsoft.EntityFrameworkCore.DbContext)">
            <summary>
            Fills audit fields (CreatedAt/CreatedBy/UpdatedAt/UpdatedBy/IsDeleted) AND
            snapshots the list of changed entries + their original state into the
            thread-local list. Snapshots are taken BEFORE SaveChanges so the OriginalValues
            are still the pre-update values; IDs are read in FlushAuditEntries AFTER
            SaveChanges so they reflect the DB-assigned auto-increment values.
            </summary>
        </member>
        <member name="M:MAIMS.Data.Interceptors.AuditSaveChangesInterceptor.FlushAuditEntries">
            <summary>
            Called AFTER SaveChanges — at this point auto-increment IDs are populated
            by the database. We re-walk the snapshotted entries, read their CurrentValues
            (which now reflect the real ID), and enqueue AuditLog entries.
            </summary>
        </member>
        <member name="T:MAIMS.Data.Interceptors.AuditSaveChangesInterceptor.PendingEntry">
            <summary>Internal record holding the entry snapshot taken in SavingChanges.</summary>
        </member>
        <member name="M:MAIMS.Data.Interceptors.AuditSaveChangesInterceptor.PendingEntry.#ctor(Microsoft.EntityFrameworkCore.ChangeTracking.EntityEntry,System.String,System.String)">
            <summary>Internal record holding the entry snapshot taken in SavingChanges.</summary>
        </member>
        <member name="T:MAIMS.Data.Interceptors.IAuditWriter">
            <summary>Deferred audit writer used by the interceptor. Implementation lives in Data layer.</summary>
        </member>
        <member name="T:MAIMS.Data.MaimsDbContext">
             <summary>
             EF Core DbContext for MAIMS. Uses Pomelo MySQL provider with utf8mb4.
             Registered as pooled DbContextFactory (not single DbContext) so each WinForms
             operation gets a short-lived context bound to its own transaction.
            
             IMPORTANT: DbContextPool requires EXACTLY ONE public constructor that takes
             a single DbContextOptions parameter. The audit interceptor is added via
             DbContextOptionsBuilder.AddInterceptors at DI registration time (see
             ServiceCollectionExtensions.AddMaimsData), NOT via constructor injection.
             </summary>
        </member>
        <member name="T:MAIMS.Data.MaimsDesignTimeFactory">
             <summary>
             Design-time factory for MaimsDbContext. Required by `dotnet ef migrations`
             and Visual Studio's Package Manager Console (Add-Migration / Update-Database)
             so they can construct a DbContext WITHOUT needing MAIMS.WinUI as startup project.
            
             This factory is ONLY used at design time (migration scaffolding + database update).
             At runtime the application uses the normal DI registration in
             ServiceCollectionExtensions.AddMaimsData().
            
             Connection string is read from appsettings.json in the MAIMS.Data project folder.
             </summary>
        </member>
        <member name="T:MAIMS.Data.Migrations.Initial">
            <inheritdoc />
        </member>
        <member name="M:MAIMS.Data.Migrations.Initial.Up(Microsoft.EntityFrameworkCore.Migrations.MigrationBuilder)">
            <inheritdoc />
        </member>
        <member name="M:MAIMS.Data.Migrations.Initial.Down(Microsoft.EntityFrameworkCore.Migrations.MigrationBuilder)">
            <inheritdoc />
        </member>
        <member name="M:MAIMS.Data.Migrations.Initial.BuildTargetModel(Microsoft.EntityFrameworkCore.ModelBuilder)">
            <inheritdoc />
        </member>
        <member name="T:MAIMS.Data.Repositories.Repository`1">
            <summary>
            Generic EF Core repository. Specific repositories (AssetRepository, etc.) can extend
            this or compose it. Read queries use AsNoTracking for performance.
            </summary>
        </member>
        <member name="T:MAIMS.Data.Seed.DbSeeder">
            <summary>
            Seeds the seven built-in roles (with default permission sets), the SystemAdministrator
            user, and a couple of departments + asset categories so the system is usable on first run.
            Idempotent — safe to call on every application start.
            </summary>
        </member>
        <member name="M:MAIMS.Data.ServiceCollectionExtensions.AddMaimsData(Microsoft.Extensions.DependencyInjection.IServiceCollection,System.String,MAIMS.Core.Abstractions.ICurrentSession)">
             <summary>
             Registers MaimsDbContext with Pomelo MySQL provider, connection pooling,
             audit interceptor, UnitOfWork, generic repository, and the asset code generator.
            
             NOTE: ICurrentSession is NOT registered here — the host (Program.cs) owns it
             because the session lifecycle depends on login state. The host must register
             ICurrentSession BEFORE calling AddMaimsData, OR pass a pre-login fallback
             via the `session` parameter for seeding-only use.
             </summary>
        </member>
        <member name="T:MAIMS.Data.UnitOfWork">
            <summary>
            Concrete UnitOfWork wrapping MaimsDbContext. Allows services to depend on
            IUnitOfWork abstraction rather than the DbContext directly (testability + transactions).
            </summary>
        </member>
        <member name="T:MAIMS.Data.AuditQueueWriter">
            <summary>
            In-process queue for pending AuditLog entries collected by the interceptor.
            The interceptor can't write directly to the DbContext during SaveChanges
            (would re-enter SaveChanges), so it queues entries here. UnitOfWork
            flushes them after a successful SaveChanges.
            </summary>
        </member>
    </members>
</doc>
